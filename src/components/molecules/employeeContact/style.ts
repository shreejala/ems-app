import {StyleSheet} from "react-native";
import {Colors} from "../../../constants/colors";

export const EmployeeContactStyle = StyleSheet.create({
  containerStyle: {
    alignItems: "center",
    paddingHorizontal: 20,
    paddingVertical: 15,
    backgroundColor: Colors.lightCyan,
    borderRadius: 20,
  },
});
